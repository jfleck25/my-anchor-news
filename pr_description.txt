💡 What:
Introduces an in-memory cache (`_file_settings_cache`) protected by a lock for the `load_settings` and `save_settings` functions when reading from `user_settings.json`.

🎯 Why:
To avoid the disk I/O and JSON parsing overhead on every call to `load_settings` when the fallback file method is used instead of the PostgreSQL database.

📊 Impact:
Reduces latency and disk reads significantly for user settings retrieval for environments relying on local JSON storage. Uses `copy.deepcopy()` to completely avoid state leakage from the cache dict to callers.

🔬 Measurement:
Running the backend tests natively shows no regressions. The change isolates and mitigates disk I/O blocking inside `load_settings` calls.
